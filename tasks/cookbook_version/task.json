{
    "id": "99b27ffc-7e88-4937-ac86-f1170b41695c",
    "name": "vsts-chef-task-cookbook-version",
    "friendlyName": "Update cookbook version number (Chef)",
    "description": "Set the version of the cookbook based on the build number",
    "helpMarkDown": "",
    "category": "Build",
    "author": "Russell Seymour",
    "version": {
        "Major": 0,
        "Minor": 0,
        "Patch": 2
    },
    "instanceNameFormat": "$(chefServerUrl)",
    "groups": [],
    "inputs": [
        {
            "name": "chefCookbookVersion",
            "type": "string",
            "label": "Cookbook Version",
            "defaultValue": "$(Build.BuildNumber)",
            "required": true,
            "helpMarkDown": "Version number to assign to the cookbook.  By default this uses the BuildNumber, which should be set to be a semantic version number"
        },
        {
            "name": "chefCookbookMetadata",
            "type": "filePath",
            "label": "Metadata file",
            "defaultValue": "$(Build.SourcesDirectory)/metadata.rb",
            "required": true,
            "helpMarkDown": "Path to the metadata.rb file to update"
        }
    ],
    "execution": {
        "Node": {
            "target": "cookbook_version.js"
        }
    }
}